<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.giftshop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".Adminstration.AdminActivity"></activity>
        <activity android:name=".SignUpPack.SignUpActivity" />
        <activity android:name=".PhoneLoginPack.PhoneLoginActivity" />
        <activity android:name=".MailLoginPack.MailLoginActivity" />
        <activity android:name=".GoogleLoginPack.GoogleLoginActivity" />
        <activity android:name=".SplashPack.SplashActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>